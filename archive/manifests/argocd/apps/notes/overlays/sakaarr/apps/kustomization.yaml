apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - "../../../base/apps"

patches:
  - target:
      group: argoproj.io
      version: v1alpha1
      kind: Application
      name: notes
    patch: |-
      - op: "replace"
        path: "/metadata/namespace"
        value: "openshift-gitops"
