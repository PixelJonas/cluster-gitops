apiVersion: keycloak.org/v1alpha1
kind: KeycloakRealm
metadata:
  name: janz
  labels:
    app: keycloak
spec:
  realm:
    id: "basic"
    realm: "basic"
    enabled: True
    displayName: "Janz"
  instanceSelector:
    matchLabels:
      app: keycloak
