kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-userworkload-monitoring
  namespace: kasten-io
spec:
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: openshift-user-workload-monitoring
  policyTypes:
    - Ingress
