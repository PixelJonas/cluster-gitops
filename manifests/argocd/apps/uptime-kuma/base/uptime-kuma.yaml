apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: uptime-kuma
  annotations:
    argocd.argoproj.io/sync-wave: "101"
spec:
  destination:
    namespace: uptime-kuma
    server: "https://kubernetes.default.svc"
  source:
    repoURL: "https://k8s-at-home.com/charts/"
    targetRevision: 1.3.4
    chart: uptime-kuma
    helm:
      values: |-

        env:
          - name: TZ
            value: Europe/Berlin
          - name: UPTIME_KUMA_PORT
            value: "3001"

        ingress:
          main:
            enabled: true
            ingressClassName: nginx
            pathType: Prefix
            hosts:
              - host: status.apps.titan.janz.digital
                paths:
                  - path: /
                    pathType: Prefix
            tls:
              - hosts:
                  - status.apps.titan.janz.digital
          external:
            enabled: true
            ingressClassName: nginx
            pathType: Prefix
            hosts:
              - host: status.janz.digital
                paths:
                  - path: /
                    pathType: Prefix
            tls:
              - hosts:
                  - status.janz.digital

        persistence:
          data:
            enabled: true
            storageClass: ceph-filesystem
            size: 3Gi
            accessMode: ReadWriteOnce

  project: cluster-apps
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
