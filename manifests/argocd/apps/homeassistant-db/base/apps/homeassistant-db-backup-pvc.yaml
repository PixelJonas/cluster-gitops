---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
  name: pvc-homeassistant-db-backup
  namespace: homeassistant-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: pmb-backup
  volumeMode: Filesystem